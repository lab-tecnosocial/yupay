---
import { Image } from 'astro:assets';
import AboutHeroImage from '../assets/blog-placeholder-about.jpg';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { url } from '../consts';
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead
			title="Nosotros — Yupay"
			description="Conoce Yupay, la plataforma boliviana de educación financiera. Nuestra misión es hacer las finanzas personales accesibles para todos."
		/>
		<style>
			/* ---- Shared ---- */
			.section-label {
				display: block;
				font-size: 0.75em;
				font-weight: 700;
				letter-spacing: 0.12em;
				text-transform: uppercase;
				color: var(--yupay-amber);
				margin-bottom: 0.6em;
			}
			.section-header {
				text-align: center;
				margin-bottom: 3em;
			}
			.section-header h2 {
				font-size: 1.9em;
				color: var(--yupay-black);
				margin: 0 0 0.5em;
			}
			.section-header p {
				color: var(--yupay-gray);
				font-size: 1em;
				margin: 0;
				max-width: 480px;
				margin-inline: auto;
			}

			/* ---- Hero ---- */
			.about-hero {
				background: var(--yupay-cream);
				padding: 4.5em 1.5em 3.5em;
				text-align: center;
				border-bottom: 3px solid var(--yupay-yellow);
			}
			.about-hero h1 {
				font-size: clamp(1.8em, 4vw, 2.8em);
				color: var(--yupay-black);
				margin: 0 0 0.5em;
			}
			.about-hero p {
				font-size: 1.1em;
				color: var(--yupay-gray);
				max-width: 540px;
				margin: 0 auto;
				line-height: 1.6;
			}

			/* ---- Image ---- */
			.about-image {
				width: 100%;
				max-height: 400px;
				overflow: hidden;
			}
			.about-image img {
				width: 100%;
				max-height: 400px;
				object-fit: cover;
				border-radius: 0;
			}

			/* ---- Name callout ---- */
			.name-callout {
				background: var(--yupay-yellow);
				padding: 3em 1.5em;
				text-align: center;
			}
			.name-callout p {
				max-width: 680px;
				margin: 0 auto;
				font-size: 1.15em;
				color: var(--yupay-black);
				line-height: 1.65;
			}
			.name-callout strong {
				font-size: 1.1em;
			}

			/* ---- Mission / Vision ---- */
			.mission-vision {
				background: white;
				padding: 5.5em 1.5em 4.5em;
			}
			.mv-grid {
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 1.5em;
				max-width: 900px;
				margin: 0 auto;
			}
			.mv-card {
				background: var(--yupay-cream);
				border-radius: 12px;
				padding: 2em;
				border-left: 4px solid var(--yupay-yellow);
				transition: transform 0.2s ease, box-shadow 0.2s ease;
			}
			.mv-card:hover {
				transform: translateY(-3px);
				box-shadow: 0 8px 24px rgba(0,0,0,0.07);
			}
			.mv-card h2 {
				font-size: 1.2em;
				color: var(--yupay-black);
				margin: 0 0 0.75em;
				display: flex;
				align-items: center;
				gap: 0.5em;
				font-weight: 700;
			}
			.mv-card p {
				color: var(--yupay-charcoal);
				font-size: 0.95em;
				line-height: 1.6;
				margin: 0;
			}

			/* ---- Focus ---- */
			.focus {
				background: var(--yupay-cream);
				padding: 5.5em 1.5em 4.5em;
			}
			.focus-grid {
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 1.5em;
				max-width: 860px;
				margin: 0 auto;
			}
			.focus-item {
				background: white;
				border-radius: 12px;
				padding: 1.75em;
				display: flex;
				gap: 1em;
				align-items: flex-start;
				border-left: 4px solid var(--yupay-yellow);
				transition: transform 0.2s ease, box-shadow 0.2s ease;
			}
			.focus-item:hover {
				transform: translateY(-2px);
				box-shadow: 0 6px 20px rgba(0,0,0,0.06);
			}
			.focus-item .icon {
				font-size: 1.7em;
				flex-shrink: 0;
			}
			.focus-item h3 {
				font-size: 1em;
				color: var(--yupay-black);
				margin: 0 0 0.3em;
				font-weight: 700;
			}
			.focus-item p {
				font-size: 0.88em;
				color: var(--yupay-gray);
				margin: 0;
				line-height: 1.55;
			}

			/* ---- CTA ---- */
			.about-cta {
				background: var(--yupay-black);
				padding: 5em 1.5em;
				text-align: center;
			}
			.about-cta h2 {
				color: white;
				font-size: 1.8em;
				margin: 0 0 0.4em;
			}
			.about-cta p {
				color: rgba(255,255,255,0.5);
				margin: 0 0 2em;
			}
			.btn-cta {
				background: var(--yupay-yellow);
				color: var(--yupay-black);
				font-weight: 700;
				padding: 0.8em 2.25em;
				border-radius: 9999px;
				text-decoration: none;
				font-size: 1em;
				transition: background-color 0.2s ease, box-shadow 0.2s ease;
				box-shadow: 0 0 22px rgba(255,184,0,0.35);
			}
			.btn-cta:hover {
				background: var(--yupay-yellow-dark);
				color: var(--yupay-black);
				box-shadow: 0 0 36px rgba(255,184,0,0.6);
			}

			@media (max-width: 720px) {
				.mv-grid,
				.focus-grid {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header />

		<!-- Hero -->
		<section class="about-hero">
			<span class="section-label">Nuestra historia</span>
			<h1>Finanzas personales al alcance de todos</h1>
			<p>
				Somos Yupay, una plataforma boliviana dedicada a hacer la educación financiera
				accesible, práctica y relevante para cada boliviano.
			</p>
		</section>

		<!-- Imagen -->
		<div class="about-image">
			<Image
				src={AboutHeroImage}
				width={1200}
				height={400}
				alt="El equipo de Yupay"
			/>
		</div>

		<!-- Nombre quechua -->
		<div class="name-callout">
			<p>
				<strong>Yupay</strong> (en quechua: <em>"contar"</em>) es más que un nombre.
				Representa nuestra convicción de que cada boliviana y boliviano merece saber
				contar su propio dinero, entender sus finanzas y construir un futuro mejor.
			</p>
		</div>

		<!-- Misión y Visión -->
		<section class="mission-vision">
			<div class="section-header">
				<span class="section-label">Lo que nos mueve</span>
				<h2>Misión y Visión</h2>
			</div>
			<div class="mv-grid">
				<div class="mv-card">
					<h2><span>🎯</span> Nuestra Misión</h2>
					<p>
						Democratizar la educación financiera en Bolivia mediante contenido claro,
						honesto y adaptado a la realidad local: desde el Fondo de Pensiones hasta
						los mercados de valores, pasando por el ahorro cotidiano.
					</p>
				</div>
				<div class="mv-card">
					<h2><span>🔭</span> Nuestra Visión</h2>
					<p>
						Un país donde cada persona —independientemente de su nivel de ingresos
						o educación formal— pueda tomar decisiones financieras informadas y
						construir un patrimonio sostenible.
					</p>
				</div>
			</div>
		</section>

		<!-- Enfoque -->
		<section class="focus">
			<div class="section-header">
				<span class="section-label">Cómo trabajamos</span>
				<h2>Nuestro enfoque</h2>
				<p>Cuatro pilares que guían todo lo que creamos</p>
			</div>
			<div class="focus-grid">
				<div class="focus-item">
					<span class="icon">🇧🇴</span>
					<div>
						<h3>Contexto boliviano</h3>
						<p>UFV, AFP, DPF, Renta Dignidad y Bolsa Boliviana de Valores. No hablamos de mercados que no son los tuyos.</p>
					</div>
				</div>
				<div class="focus-item">
					<span class="icon">📖</span>
					<div>
						<h3>Lenguaje accesible</h3>
						<p>Traducimos el lenguaje financiero a términos que cualquier persona pueda entender y aplicar.</p>
					</div>
				</div>
				<div class="focus-item">
					<span class="icon">⚡</span>
					<div>
						<h3>Acción inmediata</h3>
						<p>Cada artículo incluye pasos concretos. El conocimiento vale cuando se convierte en acción.</p>
					</div>
				</div>
				<div class="focus-item">
					<span class="icon">🤝</span>
					<div>
						<h3>Sin conflicto de interés</h3>
						<p>No vendemos productos financieros. Nuestra única motivación es que tomes mejores decisiones.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- CTA -->
		<section class="about-cta">
			<h2>¿Listo para aprender?</h2>
			<p>Explora nuestros artículos y empieza a construir tu futuro financiero hoy.</p>
			<a href={url('/blog')} class="btn-cta">Ir al blog →</a>
		</section>

		<Footer />
	</body>
</html>
